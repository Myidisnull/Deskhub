[English](PRIVACY.md) · **中文** · [Tiếng Việt](PRIVACY.vi.md)

# System Runtime 隐私政策

_生效日期：2026 年 8 月 14 日 — 版本 1.10_

> 本文是 [`PRIVACY.md`](PRIVACY.md) 的中文译本。若有出入，**以英文版为准（具有法律效力）**。

## 1. 引言

本隐私政策说明 **System Runtime**（开源项目 **Deskhub**；「应用」「我们」）在你使用 System Runtime / Deskhub 移动应用（iOS、Android）以及 Windows、macOS、Linux 桌面应用（合称「本软件」）时如何处理信息。

System Runtime 是一款远程桌面应用：它把你自己的一台计算机的屏幕流式传输到你的另一台设备，并允许你用鼠标、键盘与触摸输入控制该计算机。

本软件由个人开发者开发并发布：

- **开发者：** Manh Pham
- **联系：** manhpv151090@gmail.com
- **项目主页：** https://github.com/manhpham90vn/Deskhub

## 2. 简版摘要

**System Runtime 不收集、不存储、不出售、不共享任何个人数据。我们不运营任何服务器；通过本软件，关于你或你的使用情况的数据绝不会到达我们或任何第三方。** 没有用户账号、没有分析、没有崩溃上报、没有广告，本软件中也未嵌入任何第三方 SDK。

## 3. 本软件处理的信息

为正常工作，本软件必须在 **你自己的设备上与设备之间** 处理某些数据。其中无一传输给开发者或任何第三方。

| 数据 | 目的 | 去向 | 保留 |
|---|---|---|---|
| 被共享计算机的屏幕内容（视频帧） | 在你的另一台设备上显示该屏幕 | 直接在你的两台设备之间发送；打开 *Encrypt session traffic* 时由 System Runtime 加密会话载荷，否则仅由你自己的网络/VPN 层加密（若有） | 从不存储；仅在会话期间存在于内存 |
| 鼠标、键盘与触摸输入 | 从你的另一台设备控制被共享的计算机 | 直接从观看设备发往被共享计算机（加密开启时随会话加密） | 从不存储；注入后丢弃 |
| 你输入的地址（IP/主机名） | 连接到另一台机器 | 留在你输入它的那台设备上 | 本地保留直至你更改 |
| 你连过的最近 10 个地址、各自时间、各自使用的通行码，以及适用时是否加密与所用会话密钥 | 填充 *Recent devices* 列表以便一键重连 | 写入设备上应用自有文件夹中的 `recent-devices.txt` — Windows 为 `%USERPROFILE%\.system-runtime`，macOS 与 Linux 为 `~/.system-runtime`，iOS 与 Android 为应用沙箱 | 保留至你连上 10 个更新的地址，或你删除该文件 |
| 你的共享偏好（帧率、码率、分辨率上限、端口、共享所用网络地址、观众是否可控制本机、剪贴板同步、随系统启动、自动共享与后台模式开关、日志保留期限、可选自定义日志目录、你要求观众输入的通行码，以及启用时的会话加密开关、密钥寿命、托管选择与会话密钥） | 下次打开应用时恢复设置 | 写入同一文件夹中的 `ui-settings.txt`。在 iOS 上该文件位于应用与其广播扩展共享的 app group 容器中，以便两边对通行码、端口与加密设置一致 | 保留至你更改或删除该文件 |
| 剪贴板文本（仅在剪贴板同步开关打开且会话进行中时） | 使一台设备上复制的文本可在其它设备粘贴 | 直接在你的设备之间发送——加密开启时随会话加密，否则与其它未加密流量一样明文——每次复制上限 32 KiB；仅纯文本，从不为图像或文件 | System Runtime 从不存储；仅存在于各设备正常的系统剪贴板中 |
| 桌面应用运行时写入的诊断日志文件（`system-runtime-*.log`，较旧者压缩为 `system-runtime-*.log.gz`） | 本地排障，以及你选择发送时随缺陷报告附上 | 写入你在 Settings 中选择的日志目录；该设置为空时，macOS/Linux 为 `~/.system-runtime/`，Windows 为 `%USERPROFILE%\.system-runtime`。在这些平台的 Settings 中可见。从不自动上传 | 每个日历日一个文件，跨启动追加。超过你设定的大小（默认 10 MB）时拆分。超过压缩天数（默认 7 天）的明文日志变为 `.log.gz`。超过删除天数（默认 30 天）的文件被移除。你可在 Settings 中更改这些天数与文件夹，或自行删除文件 |
| 广播是否正在运行、已连接观众数、广播扩展自身以兆字节计的内存使用，以及最近一次启动错误文本（仅 iOS） | 让应用的共享界面报告广播扩展的状态；iOS 将其作为独立进程运行，内存过高会终止 | 写入同一 app group 容器中的 `broadcast-status.txt` | 广播结束时删除 |
| *Your name* 字段中的设备名——预填为本计算机或设备自身名称（Windows/Linux 为主机名，macOS 为计算机名，iOS 为设备名，Android 为型号），直至你编辑 | 显示在你连接的主机上、本设备地址旁，以便共享者区分观众 | 保存在同一文件夹的 `ui-settings.txt`，并在连接时发给主机——与其它流量一样明文——因此除非你换成自选名称，否则会发送该默认名；清空字段只会恢复默认名，随后仍被保存并发送。主机仅在内存中、仅在连接期间保留 | 默认为计算机或设备名；保留至你更改或删除文件。清空字段会恢复默认名，而不是去掉名字 |
| 连接统计（码率、丢包、延迟） | 适配流质量；显示在状态栏 | 仅在你的两台设备之间交换 | 从不存储；会话结束时丢弃 |

### 3.1 点对点设计

所有通信都 **直接发生在你自己的两台设备之间**，经由：

- 你的本地网络（Wi-Fi/局域网），或
- **你** 运营或订阅的 VPN（例如 Tailscale），若你选择用它经互联网访问。

我们不运营中继服务器、信令服务器或任何其它后端。本软件在技术上无法向开发者发送数据。

### 3.2 我们 **不** 处理的数据

本软件不访问或处理：你的姓名（上文所述设备名除外，其默认是你的计算机或设备自身名称）、电子邮箱、电话号码、通讯录、位置、照片、文件（你选择流式传输的 PC 屏幕上可见内容除外）、麦克风、相机、广告标识符，或超出操作系统运行应用所需之外的任何设备标识符。

### 3.3 共享手机或平板屏幕

Android 与 iOS 设备既可以查看其它机器的屏幕，也可以共享自己的屏幕。该流为 **仅观看**：入站鼠标与键盘包被丢弃，因为任一操作系统都不允许普通应用系统级驱动设备。捕获的是 **整块屏幕**，包括共享期间出现的一切——通知、其它应用、银行应用、你输入的密码。Android 每次共享都会显示系统录屏同意对话框，并在运行期间显示常驻通知；iOS 会保持系统广播指示可见。二者都是操作系统自身的信号，均可随时用来停止共享。与桌面端一样，视频只去往你的另一台设备，从不存储或发送给我们。

### 3.4 屏幕共享与远程控制的范围

共享传输的是 **整块所选显示器**：该显示器上出现的一切对已连接观众可见，包括通知、弹窗，以及你在共享期间打开的任何窗口。（单应用窗口共享已于 2026-07-27 移除；本软件现在只共享整块显示器。）当你允许远程控制时，观众的输入被注入，如同他们坐在 PC 前，可触及 **共享显示器上可见的任何应用**——不再限于单个窗口。在任何主机上，你都可以在 Settings 中完全关闭远程控制，使共享变为仅观看：到达的输入被丢弃而非注入。允许控制时仍有两项安全机制：「主机优先」——PC 前的人一碰真实鼠标或键盘，远程输入即暂停；连接结束或观众切走时，远程侧按住的键自动松开。最多五名观众可同时观看一台 PC，但同一时间只有一人驱动鼠标与键盘。

## 4. 应用请求的权限

| 平台 | 权限 | 原因 |
|---|---|---|
| iOS | 本地网络 | iOS 要求，以便与同一网络上的 PC 收发流量。仅用于流媒体会话。 |
| iOS | 屏幕录制（广播） | 仅当你从系统广播选择器开始共享本机屏幕时。iOS 每次询问，并在全程显示录制指示。 |
| Android | `INTERNET`、网络状态 | 打开到 PC 的 UDP 连接所需。仅用于流媒体会话。 |
| Android | 屏幕捕获同意（`MediaProjection`） | 仅当你开始共享本机屏幕时。Android 每次询问；答案不能被记住。 |
| Android | `FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_MEDIA_PROJECTION` | 应用在后台或屏幕关闭时保持共享。Android 对屏幕捕获要求此项。 |
| Android | `POST_NOTIFICATIONS` | 显示 Android 在屏幕共享运行期间要求的常驻通知。不发送其它通知。 |

应用不请求其它权限。若未来版本需要新权限，会在上下文中请求，并更新本政策。

## 5. 分析、广告与第三方

- **分析 / 遥测：** 无。
- **崩溃上报：** 无。诊断日志（`[DIAG]`）仅存在于你自己的机器——应用控制台输出，以及在 Windows、macOS、Linux 上，位于你在 Settings 中选择的日志目录下的纯文本（最终可为 gzip）文件；该设置为空时为 `~/.system-runtime/`（Windows 为 `%USERPROFILE%\.system-runtime`）。Settings 还控制每个文件可长多大、旧文件保留多久。它们从不上传到任何地方；只有你自行复制发送时才会离开设备，你可随时删除这些文件。
- **广告：** 无。
- **第三方 SDK：** 无。本软件仅由其自身源码（可在项目主页获取）与操作系统框架构建。
- **应用商店：** 应用通过 Apple App Store 与 Google Play 分发。Apple 与 Google 可能按其各自隐私政策收集安装/使用统计；该收集不在我们控制范围内，我们仅收到这些平台向每位开发者展示的汇总匿名统计。
- **Tailscale 或其它 VPN：** 若你选择经 VPN 连接，流量按该提供商的隐私政策处理。System Runtime 既不要求也不捆绑任何 VPN。

## 6. 安全

- 流媒体流量留在你自己的网络或你自己的 VPN 隧道内。使用 Tailscale 等 VPN 时，设备间流量由该 VPN（WireGuard）端到端加密。
- 在普通局域网上，除非打开 *Encrypt session traffic*，System Runtime 不对会话流量额外加密。每个主机在接受连接前都要求 4 位通行码——首次启动会为你生成，你可更改——但加密关闭时该码与其它流量一样明文传输。加密开启时，会话视频、输入与剪贴板加密；发现探测仍为明文；主机生成的会话密钥复制给观众，除非打开托管。设备名仍可能走明文路径并显示在主机上，请勿写入敏感内容。请仅在信任的网络上使用 System Runtime，或通过 VPN，切勿直接暴露到互联网。完整威胁模型——保护什么、不保护什么、如何报告漏洞——见 [`SECURITY.zh.md`](SECURITY.zh.md)（[英文](https://github.com/manhpham90vn/Deskhub/blob/main/SECURITY.md)）。
- `recent-devices.txt` 与 `ui-settings.txt` 中保存的通行码与会话密钥用固定密钥混淆，以免一眼可读。这不是加密，也无意防御已能访问你用户账户的人。
- 因为我们不持有关于你的任何数据，不存在可能被攻破的开发者侧数据库。

## 7. 数据保留与删除

我们什么也不保留，因此也没有什么需要我们删除。所有会话数据在会话结束时消失。应用中保存的地址可通过清空字段或卸载应用移除。最近设备列表与已保存设置——包括任何通行码与会话密钥——可通过删除应用文件夹移除（Windows 为 `%USERPROFILE%\.system-runtime`，macOS 与 Linux 为 `~/.system-runtime`），应用会在下次启动时重建为空文件夹；在 iOS 与 Android 上，卸载应用即可移除。

## 8. 你的权利（GDPR、CCPA 及类似法律）

欧盟《通用数据保护条例》（GDPR）、加州《消费者隐私法案》（CCPA）等法律赋予个人数据相关权利——访问、更正、删除、可携带、反对以及不受歧视。

因为 Deskhub / System Runtime 不收集也不持有任何个人数据，不存在可行使这些权利的数据。若你认为我们持有关于你的数据，请按下述地址联系我们，我们将在 30 日内回复。

我们不「出售」或「共享」CCPA 所定义的个人信息。

## 9. 儿童隐私

本软件不以儿童为对象，且如上所述不收集任何人的数据，包括 13 岁以下儿童（COPPA）或 16 岁以下（GDPR）。

## 10. 国际数据传输

无。通过本软件，你的数据从不离开你自己的设备与网络。

## 11. 政策变更

若本软件的数据处理实践有任何变化（例如未来版本增加可选崩溃上报），本政策将在变更发布 **之前** 更新，并附新的生效日期与下方变更记录。当前版本始终发布于：
https://github.com/manhpham90vn/Deskhub/blob/main/PRIVACY.md
（中文译本：https://github.com/manhpham90vn/Deskhub/blob/main/PRIVACY.zh.md）

| 版本 | 日期 | 变更 |
|---|---|---|
| 1.10 | 2026-08-14 | 可选会话加密：主机可加密会话视频、输入与剪贴板；启用时主机生成的会话密钥（复制/刷新、按次或持久寿命）与可选密钥托管随本地共享偏好保存；最近设备可记住连接是否加密及所用会话密钥。发现仍为明文。磁盘混淆现亦覆盖已存会话密钥。 |
| 1.9 | 2026-08-14 | 澄清应用内显示的产品名为 **System Runtime**，开源项目仍为 **Deskhub**。Windows 登录自启计划任务名、默认日志文件夹表述，以及桌面日志文件名（`system-runtime-*.log`）与 System Runtime 对齐。除这些命名外，不改变存储或传输的内容。 |
| 1.8 | 2026-08-14 | 桌面 Settings 可选择在应用启动时自动开始共享（默认关闭）。该选择与其它本地共享偏好一并存储。 |
| 1.7 | 2026-08-13 | 剪贴板同步现亦适用于 Android 与 iOS，规则与桌面相同（开关、32 KiB 上限、仅纯文本）。操作系统有限制：Android 设备仅在 Deskhub / System Runtime 为前台应用时才能读取本机剪贴板（入站文本随时可应用）；iOS 在读取新复制内容时可能显示系统粘贴提示；正在担任主机的 iOS 设备不同步剪贴板，因其广播在独立进程中运行。手机与平板也获得桌面端那种选择在哪个网络地址上共享的能力，保存在同一本地设置文件中且从不外发。除该选择外，任何设备上无新增存储。桌面会话日志改为按日历日跨启动追加同一文件，而非每次启动新建。启动仍记录本地横幅（版本、主机、地址、设置）；Share/Connect 留下条目。按大小拆分、压缩与删除不变。 |
| 1.6 | 2026-08-13 | 桌面应用增加可选剪贴板同步：开关打开时，会话中复制的纯文本在你的设备之间发送（与其它流量一样未加密，上限 32 KiB）并放入对方剪贴板；Deskhub / System Runtime 从不存储。新增本地保存的设置：共享所用网络地址、随系统启动、启动时自动共享、后台/托盘模式，以及剪贴板开关本身。打开随系统启动会创建各平台自己的启动项（Linux 的 autostart 文件、Windows 上名为 *System Runtime* 的计划任务、macOS 的 Login Item）；关闭则移除。桌面 Settings 也可将会话日志指向用户选择的绝对可写文件夹（留空则保留默认 System Runtime 文件夹）。设置与最近设备文件仍留在默认 System Runtime 文件夹。 |
| 1.5 | 2026-08-13 | 每个客户端可设置设备名（连接页的 *Your name*）。名称保存在你自己设备上现有的 `ui-settings.txt`，并在连接时发给主机——与其它流量一样未加密——以便主机在会话列表、状态行与日志中标注该观众。主机仅在内存中、仅在你连接期间保留，从不存储。字段预填为你的计算机或设备自身名称，因此除非你换成自选名称，否则会发送该默认名。桌面应用（Windows、macOS、Linux）现保留可配置的本地会话日志：按大小拆分，旧文件压缩，再按你可在 Settings 中更改的保留期删除。Settings 页也可显示日志内容并打开日志文件夹。无上传；Android 与 iOS 仍仅使用操作系统日志流。 |
| 1.4 | 2026-08-12 | iOS 上广播扩展与应用共享的状态文件现亦记录扩展自身以兆字节计的内存使用，以便共享界面显示。该值仅描述 Deskhub / System Runtime 广播进程，留在你设备上的 app group 容器内，并在广播结束时与状态文件其余部分一并删除。 |
| 1.3 | 2026-08-12 | Android 与 iOS 设备现可仅观看方式共享自己的屏幕，从而把手机或平板屏幕流到你拥有的另一台设备。这增加了各操作系统要求的屏幕捕获权限（Android 上另有前台服务及其通知），以及在 iOS 上应用与其广播扩展共享的、用于通行码与端口的 app group 容器，外加扩展写入的短暂状态文件，以便应用显示广播是否在运行。视频仍仅在你自己的设备之间传输，从不存储。 |
| 1.2 | 2026-08-07 | 通行码现为每个主机必填，首次启动生成而非留空，且每个客户端都能输入。共享设置现亦在 macOS 与 Linux（以及 Windows）上保存，最近设备列表在每个平台保存，均在应用自有本地文件夹内。无新数据离开你的设备。 |
| 1.1 | 2026-08-05 | Windows 应用现跨启动保存数据：你连过的最近 10 个地址、共享设置，以及二者所用的通行码。全部留在你自己机器上的 `%USERPROFILE%\.system-runtime`；无一外传。文档化了仅观看共享与 5 名观众上限。 |
| 1.0 | 2026-07-24 | 首次发布。 |

## 12. 联系

关于本政策或 System Runtime / Deskhub 隐私的任何问题：

- **邮箱：** manhpv151090@gmail.com
- **Issues：** https://github.com/manhpham90vn/Deskhub/issues
