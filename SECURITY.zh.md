[English](SECURITY.md) · **中文** · [Tiếng Việt](SECURITY.vi.md)

# Deskhub 安全策略

_最后更新：2026 年 8 月 14 日_

> 本文是 [`SECURITY.md`](SECURITY.md) 的中文译本。若有出入，**以英文版为准**。

开源项目为 **Deskhub**；应用内显示的产品名为 **System Runtime**。本策略对二者均适用。

## ⚠️ 请先读这段

**会话加密可选，默认关闭。** 每个主机仍要求 4 位通行码。加密关闭时，流量明文传输：谁能抓到一个包，就能读出通行码并取得对共享机器的鼠标与键盘控制。加密开启时，会话的视频、输入与剪贴板会被加密；发现探测仍为明文。主机生成会话密钥供你复制给观众，除非打开 *Escrow key to viewers*（默认亦关闭）——开启后，出示通行码的人即可从主机拿到密钥。

Deskhub 仍面向你已经信任的网络，或下层 VPN。它 **不是** 为直接暴露在公网而设计的。

因此只有一条硬性规则：

> **切勿端口转发 UDP 47777。切勿把共享机器直接暴露到互联网。远程访问请使用 VPN——本项目对照测试的是 [Tailscale](https://tailscale.com)——并连接到 `100.x.y.z` 地址。**

遵守这条规则，System Runtime 可以安全使用。打破它，等于把机器交给互联网。局域网不完全可信时请打开会话加密；能带外递交密钥时请关闭托管。产品规则见 [`docs/SPECIFICATION.md`](docs/SPECIFICATION.md) §9。

## 威胁模型

### Deskhub 能防什么

| | |
|---|---|
| 数据落到开发者手里 | 不会。没有服务器、账号、遥测、第三方 SDK。见 [`PRIVACY.zh.md`](PRIVACY.zh.md)。 |
| 远程观众与你抢机器 | 「主机优先」：你一碰真实鼠标或键盘，远程输入即暂停（Windows、macOS、Linux 主机皆然）。 |
| 按键卡死 | 远程侧正按住的键，在会话结束或观众切走时会自动松开。 |
| 无法嗅探你流量的陌生人 | 每个主机都要求通行码。同一来源的错误通行码或会话密钥尝试受限制（**60 秒**内 **5** 次失败后锁定 **30 秒**）。发现探测亦限速，避免显示器列表应答成为无限预言机。4 位空间仍然很小：若加密关闭或托管开启，局域网上坚决的扫描仍是真实风险。 |
| 可选的加密会话 | 打开 *Encrypt session traffic* 后，会话的视频、输入与剪贴板以 AEAD 加密。除非开启托管，观众需要会话密钥。加密主机拒绝明文准入。发现仍为明文。 |
| 多名观众争抢鼠标 | 最多 5 名观众可看一台主机，但同一时间只有一人驱动输入：先加入者优先，后来者的输入被丢弃，直到前者空闲满一秒。第 6 名观众以 `Busy` 拒绝。 |
| 你只想让对方看屏幕 | 仅观看共享在每个主机上都可用：主机在注入前丢弃输入包——不是靠客户端自觉。Android 与 iOS 主机无条件仅为观看。 |
| 手机误开共享未关 | 靠的是操作系统，而非 Deskhub：Android 会保持常驻通知，且每次共享都重新征求录屏同意；iOS 会保持广播指示可见。二者都能在不打开应用的情况下停止共享。 |
| 畸形数据包 | 每个字段在读取前都做边界检查。解析器有单元测试，CI 跑 AddressSanitizer、UndefinedBehaviorSanitizer、ThreadSanitizer，并 nightly 用 libFuzzer 模糊测试——六个目标覆盖线格式、H.264 解析、包重组、会话状态机与 UI 文本。模糊测试发现的崩溃会留在仓库作回归测试，新覆盖率会折回种子语料。 |

### Deskhub **不能** 防什么

下面是诚实清单。单靠通行码解决不了这些；可选会话加密能收窄一部分，但不能全部：

- **没有强机器身份。** 通行码是 4 位数字的相等比较。加密关闭时它在 `Hello` 里明文传输。加密开启且托管关闭时，会话密钥才是真正秘密——但仍无配对步骤、无主机批准提示、无地址白名单。
- **加密是可选的。** 默认流量是明文 UDP。能抓未加密会话的人可以看屏幕、读按键。打开加密只覆盖会话载荷；发现探测仍为明文。
- **托管削弱了密钥递交。** 托管开启时，出示通行码的人即可从主机拿到会话密钥。局域网敌意较强时，优先带外递交密钥。
- **设备名在未覆盖的发现/准入路径上仍可能明文传输。** 观众发送的 *Your name* 会显示在主机上并写入主机日志。默认是机器自身主机名或设备名——个人电脑上往往含有主人真实姓名。请把它当作对网络公开。
- **无抗 DoS。** 洪泛端口会打断会话；速率限制只钝化猜测，挡不住洪泛。
- **发现信标仍会应答。** `LIST_SOURCES` 探针无需会话，任意源地址都会得到回复；`PING` 亦然。通行码只是清空回复内容。限速减轻预言机滥用，但不会隐藏「这里有一台 Deskhub 主机」。
- **观众席位在 5 秒无声后自行腾出。** 若你的观众掉线，席位重新打开，下一个被准入的 `Hello` 占用它。
- **共享暴露整块显示器。** 不是单个窗口：该显示器上的每条通知、弹窗与窗口。见 [`PRIVACY.zh.md` §3.4](PRIVACY.zh.md)。
- **手机或平板主机暴露整部手机。** Android 与 iOS 也能当主机，流式传输的是整块屏幕。移动主机始终仅为观看，消除了远程控制风险，但消除不了暴露风险。

## 在哪里可以安全运行

✅ **安全**

- 你控制其上每一台设备的家庭或个人局域网。
- 仅有你自己设备加入的 Tailscale 网络（或其它 WireGuard/VPN 隧道）。即便 Deskhub 会话加密已开，VPN 仍提供网络层身份。
- 永远只当 *客户端* 的机器（从不共享屏幕的手机、平板、笔记本）。客户端不接受入站会话。

❌ **不安全 — 不要这样做**

- 在路由器上端口转发 UDP 47777，或把共享机器放进 DMZ。
- 在咖啡馆、酒店、机场、校园、联合办公或会议 Wi-Fi 上共享屏幕。
- 在你不信任其它每台设备的办公室或合租局域网共享。
- 任何有访客设备、你未配置的 IoT、或不由你管理的室友机器的网络。
- 通过云虚拟机的公网接口或公网隧道服务暴露该端口。

默认套接字绑定所有接口（`INADDR_ANY`），因此机器接入的每个网络——包括你忘了还连着的那个——都能到达。**Share on network** 设置可收窄：选中本机某一个地址后，主机只绑定该接口，其它网络上的机器甚至碰不到端口。两个注意点：若开始共享时所选地址已不存在（网线拔掉、DHCP 换了地址），Deskhub 会回退到所有接口，并在共享状态中说明——依赖此项时请看横幅；绑定单一接口也会阻止本机环回（`127.0.0.1`）观众。在 Windows 上，应用自启动起即以提升权限运行（只问一次，以便向提权窗口注入输入），并在你共享时替你打开防火墙规则——规则覆盖各配置文件下的整个应用，收窄绑定并不会收窄防火墙；正因如此，上文那条规则才重要。

## 同一网络上的攻击者能做什么

若有人与正在共享屏幕的机器处在同一局域网，且 Deskhub 在运行，他们可以：

1. 扫描 UDP 47777 发现它。通行码会清空他们拿到的显示器与分辨率列表，但机器仍会应答探针，因而仍会暴露自己。
2. **加密关闭时**：从线路上读出你的通行码并连接，立刻拥有鼠标与键盘。此后：打开浏览器、读邮件、装软件、外泄文件。也可被动录下会话，离线重建屏幕与按键。若启用剪贴板同步，它走同一明文通道。
3. **加密开启且托管关闭时**：仍能看见发现流量，但会话载荷需要会话密钥。猜密钥不现实；偷复制出的密钥或肩窥仍可能。
4. **加密开启且托管开启时**：出示通行码即可从主机拿到会话密钥——把托管当作「仅凭通行码即可进入加密会话」。

「主机优先」在你 *坐在* 机器前时限制捣乱。你不在时它帮不上忙——而这恰恰是要紧的时候。

## 加固清单

若你想按现状继续使用 Deskhub，这些值得做：

- [ ] 两边都跑 Tailscale，只通过 `100.x.y.z` 地址连接。
- [ ] 确认路由器对 UDP 47777 **没有** 端口转发或 UPnP 映射。
- [ ] 在 Settings 里把生成的通行码改成观众难以从你习惯猜出的值；只需对方观看时取消勾选 *Viewers can control this machine*。
- [ ] 在不完全信任的网络上打开 *Encrypt session traffic*，关闭 *Escrow key to viewers*，并通过带外方式复制会话密钥。一次性共享优先使用 *Per share* 密钥寿命。
- [ ] 不用时退出 Deskhub。它不是后台服务——关掉就关掉了这个洞。
- [ ] 在 Linux 上若用 `ufw`，收窄规则而不是大开：
      `sudo ufw allow from 100.64.0.0/10 to any port 47777 proto udp`，而不是
      `sudo ufw allow 47777/udp`。
- [ ] 不要让笔记本带着未结束的共享走进其它网络。
- [ ] 离开时锁定机器，以免无人值守的会话被静默接管。

## 本地产物

诊断日志以纯文本写在 `~/.system-runtime/`（Windows 上为 `%USERPROFILE%\.system-runtime`），适用于 Windows、macOS 与 Linux。其中含连接统计与对端地址，不含屏幕内容或按键。

桌面应用在该文件夹还保存两个文件：`ui-settings.txt`（帧率、码率、分辨率上限、端口、仅观看开关、主机通行码，启用加密时的会话加密开关与会话密钥材料，以及展示给主机的可选设备名——按你输入的纯文本存储）和 `recent-devices.txt`（你连过的最近 10 个地址、时间、各自使用的通行码，以及适用时是否加密与所用会话密钥）。移动应用在各自沙箱中保存同样两个文件——iOS 在 app group 容器中，以便广播扩展读取与应用界面相同的主机通行码。已存通行码与会话密钥用固定 XOR 密钥混淆，避免一眼可读或随便 `type` 文件就看到——**这不是加密**，有源码和文件的人几秒就能还原。请把该文件夹当作以你身份运行的任何程序都可读。

以上内容都不会上传；可随时删除该文件夹。

## 计划中的缓解

按拟落地顺序跟踪：

1. **主机上的连接提示** — 共享机器在接受首个会话前先询问，而不是默默接受。
2. **把通行码与会话密钥存进操作系统钥匙串**，而不是混淆后的文本文件。
3. **沉默发现信标**，使其对未经请求的探针完全不回复，而不是回空列表。

自较早修订以来已交付 / 已写入规格：每个主机上的强制通行码与仅观看开关；可选会话加密（主机生成会话密钥、复制/刷新、按次或持久寿命、可选密钥托管）；加密主机拒绝降级明文；以及 [`docs/SPECIFICATION.md`](docs/SPECIFICATION.md) §9 中的连接与发现限速。若某构建仍与规格不符，实现正在向该规格对齐。

本清单是意图声明，不是时间表。Deskhub 由一人业余维护。请把当前状态当作现状，而不是计划。

## 报告漏洞

请 **私下** 报告安全问题——不要开公开的 GitHub issue。

- **邮箱：** manhpv151090@gmail.com — 主题请写 `[Deskhub security]`。
- **或：** 在 GitHub 上开 [私有安全公告](https://github.com/manhpham90vn/Deskhub/security/advisories/new)。

请说明你运行的环境（操作系统、标题栏或 [`VERSION`](VERSION) 中的 Deskhub 版本）、你做了什么、发生了什么。附上概念验证会很有帮助。

**你可以期待：** 7 日内确认收到，30 日内完成评估。这是单人业余项目，请对时间线保持耐心——无论结论如何都会给你明确答复。若修复随版本发布，除非你不愿具名，否则会在发行说明中致谢。

没有漏洞赏金；不会付费。

**上文已写明的不算漏洞。** 缺失的认证与加密已知、已列出、正在推进——报告「没有密码也能连上 Deskhub」不会带来新信息。*值得* 报告的是：畸形包可达的内存损坏或崩溃、跳出已文档化威胁模型的路径、任何把数据弄出本机的方式，或某项缓解措施落地后其中的缺陷。

## 支持的版本

仅支持 [Releases 页面](https://github.com/manhpham90vn/Deskhub/releases) 上最新发布版。修复随新版本发布；不对旧版本回移植。

## 范围

本策略覆盖本仓库中的 Deskhub 源码，以及 Releases、TestFlight 与 Google Play 上发布的二进制。不覆盖 Tailscale、你的操作系统、路由器，或你一并运行的其它软件。
